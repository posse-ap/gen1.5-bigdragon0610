import React from "react";

const RecordButton: React.VFC = () => {
  return (
    <div>
      <button className="w-full bg-gradient-to-r from-[#0F71BC] to-[#3CCFFF] text-white text-lg py-2 rounded-r-full rounded-l-full my-5">
        記録・投稿
      </button>
    </div>
  );
};

export default RecordButton;
